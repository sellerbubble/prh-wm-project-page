# PRH-WM Project Page

This repository is a static GitHub Pages site for the NeurIPS 2026 paper
`Platonic Representation Hypothesis on World Models`.

Site URL:

```text
https://sellerbubble.github.io/prh-wm-project-page/
```

## Local Preview

Open `index.html` directly in a browser, or serve the repository root:

```bash
cd /Users/liwenhao.109/projects/claude_code/prh-wm-project-page
python -m http.server 8080
```

Then visit:

```text
http://localhost:8080
```

## GitHub Pages Setup

In the GitHub repository settings:

1. Go to `Settings -> Pages`.
2. Set source to `Deploy from a branch`.
3. Select `main`.
4. Select `/ (root)` as the publishing folder.

## Asset Provenance

The page copies selected paper assets from:

```text
neurips2026__platonic_hypothesis_on_wm/
```

The copied web assets live under:

```text
docs/assets/prh-wm/
```
